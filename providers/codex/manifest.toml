# OpenAI Codex CLI —— 官方终端 Coding Agent。
#
# 状态：manifest 已声明，Rust adapter 尚未实现（cli_adapter 留空）。
# 加 adapter 前必须先用真实 `codex --help` 核实参数格式，不臆造
# （见 runtime/crates/opc-provider/src/cli/mod.rs 的 unknown-adapter 报错）。

id = "codex-cli"
display_name = "OpenAI Codex CLI"
vendor = "OpenAI"
kind = "cli"

cli_binary = "codex"

allowed_sensitivity = ["low", "medium"]
privacy_note = "走用户本机已登录的 Codex 会话；数据流向遵循用户与 OpenAI 之间已有的服务条款。"
